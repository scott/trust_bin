<!DOCTYPE html>
<html>
  <head>
    <title>TrustBin</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <div class="container">
      <div class="row mt-3">
        <div class="col-8">
          <ul class="list-unstyled list-inline">
            <li class="list-inline-item"><h3><strong>Trust</strong>bin</h3> Secure, self hosted code snippets</li>
          <ul>
        </div>
        <div class="col-4 float-end">
          <ul class="list-unstyled list-inline">
            <li class="list-inline-item"><%= link_to 'All', pastes_path %></li>
            <li class="list-inline-item"><%= link_to 'New', new_paste_path %></li>
            <li class="list-inline-item"><%= current_user.email %></li>
            <li class="list-inline-item"><%= link_to 'Sign Out', destroy_user_session_path,
              data: {turbo_method: :delete, turbo_confirm: 'Are you sure?'} %></li>
          <ul>
        </div>
        <div class="col-md-12">

          <p class="notice"><%= notice %></p>
          <p class="alert"><%= alert %></p>
          <%= yield %>
        </>
      </div>
    </div>
  </body>
</html>
