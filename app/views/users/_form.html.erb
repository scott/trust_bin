<%= f.input :first_name %>
<%= f.input :last_name %>
<%= f.input :email %>
<%= f.input :role, collection: ["admin","user"] %>
<%= f.input :password %>
<%= f.input :password_confirmation %>
<%= f.button :submit, class: 'btn btn-primary' %> <%#= link_to '[Delete]', user_path(id: @user.id), method: :delete, class: 'btn btn-secondary', data: { confirm: 'Are you sure?' } %>
